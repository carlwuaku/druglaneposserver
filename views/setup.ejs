<%- include ('partials/header') -%>
<script src="js/angular.min.js"></script>
<section ng-app="" class="content">
    <div class="container-fluid">
  
  
      <div class="block-header">
        <h4 class="">
         Welcome to <%= name %>
          
      </h4>
      <h5>Please Setup Your Admin</h5>
      </div>
  
      <form action="saveSetup" method="POST">
        <div class="row justify-content-center">
            <div class="col-md-4">
                    <%= message %>
                    <form action="saveSetup" method="POST">
                        <div class="form-group">
                            <label  class="control-label">Admin Password</label>
                            <input ng-model="password" name="password" required type="password" class="form-control"
                               >
                        </div>
                        <div class="form-group">
                            <label class="control-label">Confirm Admin Password</label>
                            <input ng-model="confirm_password" name="confirm_password" required type="password" class="form-control"
                             >
                        </div>
                        <div ng-show="confirm_password != password && password != '' && confirm_password != ''" class="alert alert-danger">
                          Please make sure the passwords match
                        </div>
                        <button ng-disabled="confirm_password != password && password != '' && confirm_password != ''" type="submit" class="btn btn-primary btn-block">Submit</button>
                    </form>
                        
            </div>
            
            


        </div>
        

    </form>
  
  
  </div>
  
  </section>


  <%- include ('partials/footer') -%>